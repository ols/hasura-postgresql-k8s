- args:
    permission:
      backend_only: false
      filter:
        auth0_id:
          _eq: X-Hasura-User-Id
    role: user
    table:
      name: users
      schema: public
  type: create_delete_permission
